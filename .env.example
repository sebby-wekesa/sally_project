# =================================================================
# Sally Chemtai Portfolio - Environment Configuration Template
# 
# INSTRUCTIONS:
# 1. Copy this file to .env (git will ignore the actual .env file)
# 2. Update the values with your actual configuration
# 3. Never commit the .env file with real values to version control
# =================================================================

# ============= FLASK CONFIGURATION =============
# Secret key for session management - change this to a random string
SECRET_KEY=your-super-secret-key-change-in-production

# Flask environment: development, production, or testing
FLASK_ENV=production

# Enable debug mode (set to true only in development)
FLASK_DEBUG=false

# Server port
PORT=5000

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log file location
LOG_FILE=portfolio.log

# ============= DATABASE CONFIGURATION =============
# Database URL - use sqlite for development, PostgreSQL for production
# SQLite: sqlite:///portfolio.db (file-based)
# PostgreSQL: postgresql://user:password@localhost:5432/portfolio
DATABASE_URL=sqlite:///portfolio.db

# ============= EMAIL CONFIGURATION =============
# SMTP Server settings for sending emails

# Gmail configuration (recommended for development/small deployments)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true

# Your email address
MAIL_USERNAME=your-email@gmail.com

# Gmail App Password (NOT your regular password)
# Generate at: https://myaccount.google.com/apppasswords
MAIL_PASSWORD=your-app-password

# Email address to use as sender
MAIL_DEFAULT_SENDER=your-email@gmail.com

# ============= ADMIN CONFIGURATION =============
# Email address where contact form submissions are sent
ADMIN_EMAIL=sallychemtai@gmail.com

# Phone number for contact information
PHONE_NUMBER=+254 712 507368

# ============= SECURITY CONFIGURATION =============
# CSRF token secret key - change this to a random string
CSRF_SECRET_KEY=your-csrf-secret-key-change-in-production

# ============= CACHE & SESSION =============
# Redis URL for caching and session storage (optional)
# Leave as memory:// for in-memory storage
REDIS_URL=memory://

# ============= OPTIONAL: THIRD-PARTY SERVICES =============
# Google Analytics ID
GOOGLE_ANALYTICS_ID=

# Sentry DSN for error tracking (optional)
SENTRY_DSN=

# ============= DEPLOYMENT NOTES =============
# For Production:
# - Set FLASK_ENV=production
# - Set FLASK_DEBUG=false
# - Use a strong SECRET_KEY (generate with: python -c "import secrets; print(secrets.token_hex(32))")
# - Use PostgreSQL instead of SQLite
# - Configure proper email service (Gmail, SendGrid, AWS SES, etc.)
# - Enable HTTPS
# - Set up proper logging and monitoring
# - Use environment variables from your hosting platform

# For Development:
# - Set FLASK_ENV=development
# - Set FLASK_DEBUG=true
# - Can use SQLite and default mail settings
# - Log to console and file

# For Testing:
# - Set FLASK_ENV=testing
# - Use in-memory SQLite database
# - Mock email sending
